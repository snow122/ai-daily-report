# ✅ Windows定时任务配置指南

## 🎯 问题解决

**之前的问题**: 需要保持终端窗口一直开启,电脑无法正常关机  
**现在的方案**: 使用Windows任务计划程序,电脑可以正常开关机

---

## 📋 工作原理

### 之前的方式(已废弃)
```
运行 python main.py → 窗口持续运行 → 每天4点推送 → ❌ 不能关闭窗口
```

### 现在的方式(推荐)
```
Windows定时任务 → 每天4点自动触发 → 执行 python main.py --now → 推送后结束 → ✅ 无需保持运行
```

---

## 🚀 配置步骤(超简单!)

### 方法1: 一键配置(推荐)

1. **右键点击** `setup_task.bat`
2. 选择 **"以管理员身份运行"**
3. 等待提示 "✅ 定时任务创建成功!"
4. 完成!

### 方法2: 手动配置

打开**管理员权限**的CMD或PowerShell,运行:

```bash
schtasks /create ^
    /tn "AI_Daily_Report" ^
    /tr "python main.py --now" ^
    /sc daily ^
    /st 16:00 ^
    /ru %USERNAME% ^
    /f
```

在目录: `d:\桌面\项目\ai-daily-report`

---

## ⏰ 任务详情

- **任务名称**: AI_Daily_Report
- **执行时间**: 每天下午 16:00 (4点)
- **执行命令**: `python main.py --now`
- **工作目录**: `d:\桌面\项目\ai-daily-report`
- **执行频率**: 每天一次

---

## 🔍 查看和管理任务

### 图形界面方式

1. 按 `Win + R`,输入 `taskschd.msc`,回车
2. 在左侧找到 **"任务计划程序库"**
3. 找到 **"AI_Daily_Report"** 任务
4. 可以查看、启用/禁用、删除任务

### 命令行方式

**查看任务状态**:
```bash
schtasks /query /tn "AI_Daily_Report"
```

**立即测试执行**:
```bash
schtasks /run /tn "AI_Daily_Report"
```

**删除任务**:
```bash
schtasks /delete /tn "AI_Daily_Report" /f
```

**禁用任务**(临时停止):
```bash
schtasks /change /tn "AI_Daily_Report" /disable
```

**重新启用任务**:
```bash
schtasks /change /tn "AI_Daily_Report" /enable
```

---

## 💡 使用场景

### ✅ 适合的场景

- 电脑每天会关机/重启
- 不想保持终端窗口开启
- 希望系统级别自动化
- 电脑在下午4点通常是开机状态

### ❌ 不适合的场景

- 下午4点电脑经常关机(建议改为其他时间)
- 需要实时监控任务执行状态

---

## 🔧 修改执行时间

如果想改为其他时间(比如早上9点):

### 方法1: 删除重建

```bash
# 删除旧任务
schtasks /delete /tn "AI_Daily_Report" /f

# 创建新任务(改为09:00)
schtasks /create ^
    /tn "AI_Daily_Report" ^
    /tr "python main.py --now" ^
    /sc daily ^
    /st 09:00 ^
    /ru %USERNAME% ^
    /f
```

### 方法2: 使用图形界面

1. 打开 `taskschd.msc`
2. 右键点击 "AI_Daily_Report"
3. 选择 "属性"
4. 切换到 "触发器" 标签
5. 编辑触发器,修改时间

---

## 📅 明天会发生什么?

**如果下午4点时电脑是开着的**:
1. Windows自动触发任务
2. 执行 `python main.py --now`
3. 获取最新AI论文和资讯
4. 生成报告并推送到飞书
5. 保存报告到文件
6. 任务结束(无窗口残留)

**如果下午4点时电脑是关机的**:
- 任务不会执行
- 下次开机会错过这次推送
- 后天会继续正常推送

**补救措施**:
如果错过了,随时手动运行:
```bash
cd /d "d:\桌面\项目\ai-daily-report"
python main.py --now
```

---

## 🎁 额外优化(可选)

### 确保Python路径正确

如果任务执行失败,可能是Python路径问题。

**解决方案**: 使用Python的完整路径

1. 找到Python安装路径:
   ```bash
   where python
   ```
   例如: `C:\Users\kele\AppData\Local\Programs\Python\Python312\python.exe`

2. 删除旧任务:
   ```bash
   schtasks /delete /tn "AI_Daily_Report" /f
   ```

3. 用完整路径创建任务:
   ```bash
   schtasks /create ^
       /tn "AI_Daily_Report" ^
       /tr "C:\Users\kele\AppData\Local\Programs\Python\Python312\python.exe d:\桌面\项目\ai-daily-report\main.py --now" ^
       /sc daily ^
       /st 16:00 ^
       /ru %USERNAME% ^
       /f
   ```

### 添加日志记录

任务执行时会生成日志到:
- `logs/ai_report.log` - 应用日志
- `reports/ai_report_YYYYMMDD.md` - 每日报告

可以通过这些文件确认任务是否正常执行。

---

## ❓ 常见问题

### Q1: 任务创建失败?

**A**: 确保以**管理员身份**运行脚本或命令。

### Q2: 任务执行了但没收到消息?

**A**: 
1. 检查 `logs/ai_report.log` 查看错误
2. 确认 `.env` 文件中Webhook URL正确
3. 手动运行 `python main.py --now` 测试

### Q3: 如何让任务在多个时间执行?

**A**: 创建多个任务,或使用图形界面添加多个触发器。

### Q4: 任务会在睡眠模式下执行吗?

**A**: 不会。如果需要,可以在任务属性的"条件"标签中勾选"唤醒计算机运行此任务"。

---

## 🎉 总结

**优势**:
- ✅ 电脑可以正常开关机
- ✅ 无需保持任何窗口开启
- ✅ 系统级别自动化,稳定可靠
- ✅ 易于管理和维护

**下一步**:
1. 右键运行 `setup_task.bat`(管理员身份)
2. 看到 "✅ 定时任务创建成功!"
3. 完成!明天下午4点见! 🎊
