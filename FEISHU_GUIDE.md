# 📱 飞书机器人配置指南

## 第一步: 创建飞书群机器人

### 方法一: 在群聊中创建

1. **打开飞书**,进入你想要接收报告的群聊
2. 点击右上角的 **"..."** 或 **设置图标**
3. 选择 **"添加机器人"** 或 **"智能助手"**
4. 点击 **"添加机器人"** 按钮
5. 在机器人列表中,找到并点击 **"自定义机器人"**
6. 配置机器人信息:
   - **名称**: AI日报 (或你喜欢的名字)
   - **描述**: 自动推送AI领域每日进展
   - **头像**: 可选上传
7. 点击 **"完成"** 或 **"添加"**

### 方法二: 通过群组设置创建

1. 在群聊中,点击右上角 **"..."**
2. 选择 **"群机器人"**
3. 点击 **"+"** 或 **"添加机器人"**
4. 选择 **"自定义机器人"**
5. 按照提示完成创建

## 第二步: 获取 Webhook URL

创建成功后,飞书会显示一个 **Webhook 地址**,格式如下:

```
https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
```

**重要**: 请妥善保管这个地址,不要泄露给他人!

点击 **"复制"** 按钮,将 Webhook URL 复制到剪贴板。

## 第三步: 配置到项目中

### 1. 创建 .env 文件

在项目根目录下,复制模板文件:

```bash
cp .env.example .env
```

### 2. 编辑 .env 文件

用文本编辑器打开 `.env` 文件,填入你的 Webhook URL:

```env
# 飞书 Webhook URL (推荐)
FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/你的实际webhook_key_here

# 其他配置留空或删除
```

**注意**:
- 只保留 `FEISHU_WEBHOOK_URL` 这一行
- 将 `你的实际webhook_key_here` 替换为你从飞书复制的真实 key
- 确保 URL 完整,没有多余空格

### 3. 保存文件

保存 `.env` 文件。

## 第四步: 测试推送

运行测试命令:

```bash
python main.py --now
```

如果配置正确,你应该在飞书群聊中收到一条卡片消息,包含:
- 🤖 AI Daily Report 标题
- 今日论文数量
- 技术动态数量
- 市场资讯数量
- 部分论文和新闻标题

## 第五步: 启动定时任务

确认测试成功后,正式启动定时任务:

```bash
python main.py
```

程序将在每天早晨 8:00 自动推送报告到你的飞书群。

## 🎨 飞书消息样式

系统使用飞书的 **卡片消息(Interactive Card)** 格式,具有以下特点:

- ✅ 支持 Markdown 格式化
- ✅ 美观的卡片布局
- ✅ 蓝色主题 header
- ✅ 清晰的层级结构
- ✅ 可点击的链接

## 🔧 常见问题

### Q1: 收不到消息?

**检查清单**:
1. 确认 Webhook URL 是否正确复制
2. 确认 `.env` 文件已保存
3. 确认机器人仍在群聊中(未被移除)
4. 查看 `logs/ai_report.log` 日志文件

### Q2: 如何修改机器人名称?

1. 在群聊中右键点击机器人
2. 选择 "机器人设置"
3. 修改名称和头像

### Q3: 可以推送到多个群吗?

目前版本只支持一个 Webhook。如需多群推送,可以:
- 创建多个机器人,分别配置不同的项目实例
- 或者使用飞书的群转发功能

### Q4: 消息能否@特定人?

当前版本暂不支持 @ 功能。如需此功能,可以在飞书机器人设置中开启 "@所有人" 权限。

### Q5: 如何停止推送?

- **临时停止**: 关闭运行程序的终端窗口
- **永久停止**: 在群聊中移除机器人

## 📊 示例消息预览

你在飞书中收到的消息类似这样:

```
┌─────────────────────────────────────┐
│ 🤖 AI Daily Report                  │
├─────────────────────────────────────┤
│                                     │
│ 📚 Latest Papers / 最新论文: 8 篇   │
│   1. GPT-5 Technical Report         │
│   2. Advances in Computer Vision    │
│   3. New Breakthrough in NLP        │
│                                     │
│ 💻 Tech Updates / 技术动态: 5 条    │
│ 📈 Market News / 市场资讯: 4 条     │
│                                     │
│ 完整报告已生成,请查看文件或使用      │
│ Web界面浏览。                        │
│                                     │
└─────────────────────────────────────┘
```

## 🎉 完成!

现在你已经成功配置了飞书推送,每天早晨 8 点都会准时收到 AI 领域的最新进展!

如有其他问题,请查看项目 README.md 或 QUICKSTART.md 文档。
